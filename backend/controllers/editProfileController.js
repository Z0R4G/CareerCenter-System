
const UpdateuserProfile = async (req, res) => {
    const { id, email, year, program, gender } = req.body;
    try {
        if (!id || !email || !year || !program || !gender) {
            
        }} catch (err) {}};
        
module.exports = {
    UpdateuserProfile
};